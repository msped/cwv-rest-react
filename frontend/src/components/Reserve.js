import React from 'react'
import {
    Button,
} from '@mui/material'

export default function Reserve() {
  return (
    <React.Fragment>
        <Button variant='contained'>
            Reserve Vehicle
        </Button>
    </React.Fragment>
  )
}
